# Dashboard Management

Serves can be fully managed from the web dashboard without touching the CLI.

Navigate to **Serves** in the sidebar to see all active serves across the organization. You can also manage serves from a machine's detail page under the **Serves** tab.

From the dashboard, you can create new serves (selecting the machine, port, protocol, internal hostname, and ACL rules), monitor their status, and delete them.
