# tuntun dns

Manage PeerDNS.

## Usage

```bash
tuntun dns status
```

## Output

Shows the current DNS configuration including the DNS suffix, upstream resolvers, and cached entries. Useful for debugging hostname resolution issues.
